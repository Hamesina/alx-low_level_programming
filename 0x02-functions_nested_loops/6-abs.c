#include "main.h"
/**
 * _abs - absolute value
 *
 * @int: integer
 * Return: number
 */
int _abs(int)
{
	int n;

	if (n > 0)
	{
		n = n;
	}
	else if (n == 0)
	{
		n = 0;
	}
	else
	{
		n = n * (-1);
	}
	return (n);
}
